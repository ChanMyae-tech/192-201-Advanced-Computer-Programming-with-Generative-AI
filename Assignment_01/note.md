## Questions I asked AI
1. What does AttributeError mean?
2. What does RecursionError mean?
3. Why should the temperature validation use "or" instead of "and"?
4. Why does is_energy_saving need to use the current temperature?
5. Why does the constructor need to use `self.temperature = temperature` instead of `self._temperature = temperature`?
6. Why does the `cooler()` method need to stop at the minimum temperature?
